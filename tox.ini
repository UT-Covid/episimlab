[tox]
isolated_build = true
envlist = py38, py37

[testenv]
allowlist_externals = 
    poetry
commands =
    poetry install 
    poetry run python -c 'import episimlab'
    poetry run pytest -n 4 {posargs}
